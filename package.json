{
  "name": "byvernacchia",
  "version": "1.0.0",
  "description": "personal website",
  "author": "Alex Vernacchia",
  "license": "MIT",
  "private": true,
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel index.html -p 8080",
    "serve": "serve -p 8080 dist/",
    "build": "rm -rf ./dist && parcel build index.html && cp surprise.html ./dist"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "parcel": "^2.8.3",
    "tippy.js": "^6.2.4",
    "typeit": "^8.7.0"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.8.3",
    "@parcel/transformer-svg": "^2.8.3",
    "serve": "^14.0.1"
  }
}
